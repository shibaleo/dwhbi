---
title: 障害対応フロー
description: インシデント発生時の対応手順
---

# 障害対応フロー

## 方針

障害検知から復旧までのフローを「検知 → 影響範囲特定 → 暫定対応 → 根本原因分析 → 恒久対応 → 振り返り」の6段階で定義する。個人開発のため即座にエスカレーションは不要だが、対応履歴は GitHub Issues に記録する。よくある障害パターン（トークン期限切れ、API 変更、レート制限）は runbook に手順を記載済み。重大障害は ADR として設計変更を検討する。
