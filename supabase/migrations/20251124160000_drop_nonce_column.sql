-- =============================================================================
-- credentials.services から nonce カラムを削除
-- =============================================================================
-- 
-- nonce は credentials_encrypted の先頭12バイトに含まれるため、
-- 別カラムは不要
-- =============================================================================

ALTER TABLE credentials.services DROP COLUMN IF EXISTS nonce;
