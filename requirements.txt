# Production dependencies
httpx>=0.27.0
supabase>=2.0.0
psycopg2-binary>=2.9.0  # Required for Vault direct connection
cryptography>=42.0.0
python-dotenv>=1.0.0
tzdata>=2024.1  # Required for zoneinfo on Windows
dbt-postgres>=1.8.0  # dbt for PostgreSQL (transform layer)
mashumaro>=3.17  # Required for Python 3.14 compatibility with dbt

# Development dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
ruff>=0.3.0
